# Fixture-local minima (copy of shape from domains/medical; path is caller-supplied)
genre: fixture-notfall
schema_version: "0.1"
catalog_version_hint: "0.1.0"
distinct_forms_min: 4
early_gatekeeper_forms:
  - cave
  - memo
early_window:
  mode: first_n_catalog_forms
  n: 3
forbidden_sole_forms:
  - styled-table
forbid_only_table_body: true
