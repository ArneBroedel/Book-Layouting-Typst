# Form Lab assets (lab-only)

**Not gold / CANONICAL production slugs.** These trees support Chapter Form Lab (V/R passes, Pass O open-asset packs).

| | |
|---|---|
| **Path** | `domains/medical/assets/form-lab/<lab-id>/` |
| **Stable** | **Do not relocate** — many `chapter.typ` files use absolute mounts `/domains/medical/assets/form-lab/…` |
| **Boards** | `toolset/orchestration/form-lab/` (+ `_archive/`) |
| **Compose** | `toolset/compose/lab/form-lab-*` |
| **Gold assets** | sibling dirs under `domains/medical/assets/<slug>/` — see [`../CANONICAL.md`](../CANONICAL.md) |

Agent default: load only when Form Lab (studio 9) or open-assets Pass O dogfood needs them.
