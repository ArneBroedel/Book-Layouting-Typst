# Pinit Showcase Phase 1: Foundation and Basics

## Goals
1.  Initialize a dedicated section for Pinit inside `src/chapters/pinit-showcase.typ`.
2.  Add a `card()` container with the introductory notes.
3.  Implement basic `#pin()`, `#pinit-rect()`, and `#pinit-highlight()`.

## Acceptance Criteria
- [x] A new `.typ` file is created and referenced in `main.typ` or appended to `12-beyond-flow.typ`.
- [x] Visual Evaluation: Run `typst compile --format png` and visually confirm that Highlights perfectly encircle the target words.
- [x] **MANDATORY:** Code must follow the Boilerplate (`[empty line] #pinit-xxx [line] #box()`) from `Guides/Pinit-Best-Practices.md`. Avoid compilation regressions.
